{
  "name": "command-center-control-plane",
  "version": "1.0.0",
  "private": true,
  "description": "Central ingest/state/stream service for Command Center",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "sync:adapters": "node scripts/sync-adapters-from-operator-config.js",
    "bootstrap": "npm run migrate && npm run sync:adapters",
    "gen:key": "node scripts/generate-ingest-key.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
