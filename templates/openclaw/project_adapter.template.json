{
  "project_id": "{{PROJECT_ID}}",
  "name": "{{PROJECT_NAME}}",
  "type": "{{PROJECT_TYPE}}",
  "owner": "{{OWNER}}",
  "priority": "{{PRIORITY}}",
  "aliases": [
    "{{PROJECT_ID}}",
    "{{PROJECT_NAME_SLUG}}"
  ],
  "heartbeat_interval_seconds": {{HEARTBEAT_INTERVAL_SECONDS}},
  "max_event_age_seconds": {{MAX_EVENT_AGE_SECONDS}},
  "required_metrics": [
{{REQUIRED_METRICS_JSON_LINES}}
  ],
  "severity_map": {
    "ok": "ok",
    "info": "ok",
    "warn": "warn",
    "warning": "warn",
    "critical": "critical",
    "error": "critical",
    "stale": "critical"
  },
  "runbook_links": {
    "reviews": "{{RUNBOOK_REVIEWS}}",
    "solve": "{{RUNBOOK_SOLVE}}",
    "prevent": "{{RUNBOOK_PREVENT}}",
    "rollback": "{{RUNBOOK_ROLLBACK}}"
  },
  "event_types": [
    "heartbeat",
    "metric",
    "action",
    "incident"
  ]
}
